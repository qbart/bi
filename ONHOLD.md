This is a list of feature ON HOLD - do not implement.

## better substitute

& — repeat last substitute on current line


:g/pattern/d — delete all matching lines; :v/pattern/d — keep only matching
:g/TODO/normal A // fixme — run a normal command on every match
- these are example, but it is nice to run commands as batch


## Formatting & misc

gq + motion — reflow text to textwidth
== / =G — reindent line / to end of file
